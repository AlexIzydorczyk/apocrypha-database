<%= form_with(model: apocryphon) do |form| %>
  <% if apocryphon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(apocryphon.errors.count, "error") %> prohibited this apocryphon from being saved:</h2>

      <ul>
        <% apocryphon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
