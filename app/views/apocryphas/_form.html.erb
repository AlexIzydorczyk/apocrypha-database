<%= form_with(model: apocrypha) do |form| %>
  <% if apocrypha.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(apocrypha.errors.count, "error") %> prohibited this apocrypha from being saved:</h2>

      <ul>
        <% apocrypha.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
