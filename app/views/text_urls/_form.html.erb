

<%= form_with(model: text_url) do |form| %>
  <% if text_url.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(text_url.errors.count, "error") %> prohibited this text_url from being saved:</h2>

      <ul>
        <% text_url.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="form-group col-4 mb-2">
		    <%= form.label :type, style: "display: block" %>
	    <%= form.text_field :type, class: "form-control" %>
		</div>
  <div class="form-group col-4 mb-2">
		    <%= form.label :text_id, style: "display: block" %>
	    <%= form.text_field :text_id, class: "form-control" %>
		</div>
  <div class="form-group col-4 mb-2">
		    <%= form.label :url, style: "display: block" %>
	    <%= form.text_field :url, class: "form-control" %>
		</div>
</div>
  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
	