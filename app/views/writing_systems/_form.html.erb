

<%= form_with(model: writing_system) do |form| %>
  <% if writing_system.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(writing_system.errors.count, "error") %> prohibited this writing_system from being saved:</h2>

      <ul>
        <% writing_system.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="form-group col-4 mb-2">
		    <%= form.label :name, style: "display: block" %>
	    <%= form.text_field :name, class: "form-control" %>
		</div>
</div>
  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
	