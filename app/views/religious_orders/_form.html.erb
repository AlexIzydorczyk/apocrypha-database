

<%= form_with(model: religious_order) do |form| %>
  <% if religious_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(religious_order.errors.count, "error") %> prohibited this religious_order from being saved:</h2>

      <ul>
        <% religious_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="form-group col-4 mb-2">
		    <%= form.label :order_name, style: "display: block" %>
	    <%= form.text_field :order_name, class: "form-control" %>
		</div>
</div>
  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
	